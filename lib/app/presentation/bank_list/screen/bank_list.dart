import 'package:flutter/material.dart';

class BankList extends StatelessWidget {
  const BankList({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Banka Listesi')),
    );
  }
}
